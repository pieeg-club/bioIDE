{
  "name": "bioide",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @bioide/demo",
    "build": "npm run build -w @bioide/demo",
    "preview": "npm run preview -w @bioide/demo"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
